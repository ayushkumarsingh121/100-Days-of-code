// ### **7. Using Object.keys()**
// **Question:**
// Given the object `car = { brand: "BMW", model: "X5" }`, use `Object.keys()` to log the keys of the object.

// **Expected Output:**

// `["brand", "model"]`


let car = {
  brand : "BMW",
  model : "X5"
}

console.log(Object.keys(car))