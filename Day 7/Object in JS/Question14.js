// ### **14. Object Constructor**

// **Question:**

// Create an empty object using the `Object()` constructor and then add a `color` property with the value `blue`. Log the object.

// **Expected Output:**

// `{ color: "blue" }`


let obj = new Object()
obj.color = "blue"
console.log(obj)