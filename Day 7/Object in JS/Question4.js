// ### **4. Adding Properties to an Object**

// **Question:**

// Create an object `student = { name: "Alice" }` and then add a property `age` with the value `20`. Log the updated object.

// **Expected Output:**

// `{ name: "Alice", age: 20 }`


let student = { 
   name: "Alice" 
}

student.age = 20
console.log(student)